# Nginx configuration for Railway (if using nginx)
# This file may be needed if Railway uses nginx as a reverse proxy

client_max_body_size 100M;
proxy_read_timeout 300s;
proxy_connect_timeout 300s;
proxy_send_timeout 300s;

